<p id="notice"><%= notice %></p>
<h1>Food Items</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Description</th>
      <th>Section</th>
      <th>Image url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @food_items.each do |food_item| %>
      <tr>
        <td><%= food_item.name %></td>
        <td><%= food_item.price %></td>
        <td><%= food_item.description %></td>
        <td><%= food_item.section %></td>
        <td><%= food_item.image_url %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br> -->

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Image</th>
    </tr>
  </thead>
  <tbody>
    <% @food_items.each do |food_item| %>
      <tr>
        <td><%= food_item.name %></td>
        <td><%= number_to_currency food_item.price %></td>
        <td><%= image_tag food_item.image_url_or_default %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Food Item', new_food_item_path %>
